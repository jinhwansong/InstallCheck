# 로컬 3D 가져오기 구현·검증

2026-09-10: GLB/STL/OBJ를 설치된 Three.js 로더로 읽어 정적 메시로 배치한다. 새 의존성이나 외부 업로드 API는 추가하지 않았다. 단위와 상향 축은 사용자가 확인한다. 실제 정밀 형상 충돌 대신 기존 외곽 상자 검사 규칙을 적용하며 화면과 검토서에 한계를 표시한다.

메시는 정규화 좌표로 프로젝트에 한 번 보존하고 구성요소와 검사 스냅샷이 ID로 참조한다. JSON 복원 시 좌표 범위, 전체 크기, 참조 무결성을 검사한다. 기존 schema 1 자료는 계속 읽는다. 파일 크기·면 수 한도와 Worker 시간 제한을 두고 GLB 외부 리소스는 거부한다.

검증: Node 기본 테스트 러너 15개 통과. OBJ 단위/상향 축, ASCII STL, 바이너리 GLB, 외부 참조 거부, JSON/이력 메시 참조를 포함한다. Edge 실제 브라우저에서 샘플 OBJ 업로드, 치수 확인, 설비 추가, 검사 저장, 새로고침 복원, 빈 파일 오류, 모바일 가로 넘침 없음, 외부 네트워크 요청 없음 확인. 기존 배치/되돌리기/이력/JSON/인쇄 브라우저 회귀 검사도 통과했다.

다양한 제조사 CAD 파일 전체 호환성을 검증한 것은 아니다. STEP/DWG, 사진 생성, 정밀 삼각형 간섭, 텍스처·애니메이션과 서버 보안은 이번 범위에 없다.

## 공장 배경과 기계 배치 확장

공장 파일은 단일 반투명 고정 배경으로 보존하고 기계와 구분한다. 배경 위치·회전·치수 편집, 표시/숨김, 제거를 지원한다. 벽·기둥 등록은 기존 장애물 검사에 연결했다. 배경 자체와 배경 내 객체는 자동 검사·분리하지 않는다. 이 제한은 항상 미확인 항목 및 검토서에 남긴다.

검증: 자동 테스트 16개 통과. 공장 추가가 기존 기계 충돌 결과를 바꾸지 않는지, 배경 참조와 검사 스냅샷 복원, 배경 편집 후 재검토 감지, 잘못된 참조·무한대 거부를 포함한다. Edge에서 공장 가져오기·숨김·정렬·되돌리기, 기계 위치·회전·복제, 벽 등록, 검토서, 배경 삭제 후 이력 및 schema 3 유지, 새로고침 복원, 모바일 가로 넘침 없음을 확인했다.

공장 배경을 제거해도 schema 3을 유지한다. 과거 검사에 남은 배경을 이전 앱이 조용히 누락하지 않도록 하기 위함이다. 메시 그리기는 공장과 기계가 동일 함수를 재사용한다. 신규 의존성은 없다.

2026-09-16 최종 확인: 위 16개 테스트와 프로덕션 빌드, 공장 배치 및 기존 기능의 Edge 브라우저 회귀 검사를 재실행해 통과했다. 별도 lint 명령은 구성되어 있지 않다. Vite는 기존 Three.js 포함 주 번들의 500 kB 초과 안내를 출력한다.

검토 범위: 28602d6 이후 변경. 재사용·품질·효율성 검토를 작업 지침에 따라 메인 컨텍스트에서 순차 수행했다. 공장 메시가 선택·드래그 대상과 충돌 대상에 포함되지 않는지, 저장된 배경 참조와 schema 유지, 사용자 파일명 출력의 이스케이프, 자원 해제, 보고서 범위 표시를 확인했다. 미해결 차단 결함은 발견하지 않았다.

Code review: skipped (ce-code-review unavailable) — focused 경로의 독립 검토가 필요하지만 이 작업의 AGENTS 지침은 위임을 메인 컨텍스트 순차 작업으로 대체한다. 독립 검토 완료로 기록하지 않고 수동 diff 검토 및 실제 브라우저 검증으로 대체했다.
